import { type NextPage } from "next";
import Layout from "@/components/Layout";

const Home: NextPage = () => {
  return (
    <Layout title={"App | Huddle"}>
      <div>game</div>
    </Layout>
  );
};

export default Home;
